__pycache__/
*.pyc
build/
*.lattice
*.wal

# Receipts are generated per machine by `make receipt`. We do not ship them —
# a receipt you did not produce tells you nothing about your hardware.
receipts/generated/
